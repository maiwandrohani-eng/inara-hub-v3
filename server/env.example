# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/inara_platform?schema=public"

# Server Configuration
PORT=5000
NODE_ENV=production

# Authentication
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-min-32-characters"
JWT_EXPIRES_IN="7d"

# CORS Configuration (comma-separated list of allowed origins)
# For production, specify your frontend domain(s)
# Example: CORS_ORIGIN="https://yourdomain.com,https://www.yourdomain.com"
# Leave empty or set to "*" to allow all origins (not recommended for production)
CORS_ORIGIN=""

# AI Integration (Optional - for course generation and bot)
AI_API_KEY=""
AI_API_URL="https://router.huggingface.co/v1/chat/completions"
AI_MODEL="mistralai/Mistral-7B-Instruct-v0.2"

# File Upload Configuration
# Max file size is configured in code (100MB)
# Max JSON body size is configured in code (10MB)

